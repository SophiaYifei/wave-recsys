# All versions are pinned to the set actually used to produce the committed
# eval_results.json / models / features.npz. Groups below indicate which
# packages the production backend actually needs vs which are only used by
# the offline data-collection + training pipelines under scripts/.

# -------------------------------------------------------------------
# Core inference — imported by app/backend/** at request time
# -------------------------------------------------------------------
fastapi==0.136.0
uvicorn[standard]==0.44.0
pydantic==2.13.2
python-dotenv==1.2.2
openai==2.32.0
sentence-transformers==5.4.1
torch==2.11.0
numpy==2.4.4
huggingface_hub==1.11.0
requests==2.33.1

# -------------------------------------------------------------------
# Data pipeline + training — imported by scripts/** only
# (not required to serve the pre-trained model)
# -------------------------------------------------------------------
pandas==3.0.2
scipy==1.17.1
scikit-learn==1.8.0
matplotlib==3.10.8
seaborn==0.13.2
feedparser==6.0.12
beautifulsoup4==4.14.3
spotipy==2.26.0
tqdm==4.67.3
pillow==12.2.0
jsonlines==4.0.0
faiss-cpu==1.13.2
