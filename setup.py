from setuptools import setup, find_packages

setup(
    name="wave",
    version="0.1.0",
    description="Cross-modal mood-aligned content recommender.",
    packages=find_packages(),
    python_requires=">=3.10",
)
